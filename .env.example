# Vercel Postgres (auto-set by Vercel, or set manually for local dev)
POSTGRES_URL=postgres://user:pass@host:5432/dbname

# JWT secret (generate a random string for production)
JWT_SECRET=change-me-in-production
